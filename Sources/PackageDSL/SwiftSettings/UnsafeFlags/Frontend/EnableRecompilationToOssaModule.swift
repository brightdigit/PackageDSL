/// Passes the flag `-enable-recompilation-to-ossa-module`
///
/// Allow recompilation of a non-OSSA module to an OSSA module when imported from
/// another OSSA module
public struct EnableRecompilationToOssaModule: FrontendFlag {}
