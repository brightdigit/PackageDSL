/// Passes the flag `-reflection-metadata-for-debugger-only`
///  Emit reflection metadata for debugger only, don't make them available at runtime
public struct ReflectionMetadataForDebuggerOnly: FrontendFlag { }
