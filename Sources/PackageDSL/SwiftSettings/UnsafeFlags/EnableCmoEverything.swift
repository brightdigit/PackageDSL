/// Passes the flag `-enable-cmo-everything`
///
/// Perform cross-module optimization on everything (all APIs). This is the same
/// level of serialization as Embedded Swift.
public struct EnableCmoEverything: UnsafeFlag {}
