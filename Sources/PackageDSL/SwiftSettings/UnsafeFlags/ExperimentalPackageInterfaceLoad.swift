/// Passes the flag `-experimental-package-interface-load`
///  Enables loading a package interface if in the same package specified with package-name
struct ExperimentalPackageInterfaceLoad: UnsafeFlag { }
