name: OSVer
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  OSVer:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}